## 可能原因
- Vercel 项目仍指向旧仓库或旧分支，未获取最新提交。
- 框架预设选择错（Next.js），导致忽略根目录 `index.html`。
- 浏览器缓存未刷新，页面仍显示旧部署。

## 快速验证
1. 在 Vercel 项目 → Deployments 查看最新部署的 Commit 是否为 `main` 的最新（包含修改后的 `index.html` 标题“Web3大航海”）。
2. 访问 `https://.../index.html?nocache=1` 或用隐私模式强制刷新，确认是否仍是旧内容。
3. 打开 `https://.../ai-invest`，应跳转到工具页（由 `ai-invest/index.html`）。

## 修复步骤
1. 在 Vercel 项目 Settings → Git：确保项目绑定的是 `CPANHAHA/art-history` 仓库，Production Branch 设为 `main`，开启 Auto Deploy。
2. 在 Settings → General：Framework Preset 选择“Other”，Build Command 留空，Output Directory 留空，使根目录静态文件与 Functions 生效。
3. 重新部署：点击 Redeploy latest 或从仓库做一次空提交触发部署。
4. 验证首页标题与导航：
   - 标题应为“Web3大航海”（`index.html:6,11-17`）。
   - 导航包含“AI投研助手”和“管理后台”（登录管理员后显示）。
5. 若仍显示旧页面，检查是否访问了旧项目域名（名称包含 `art-history` 的历史项目）；切换到正确项目域名或在 Domains 绑定自定义域名。

## 代码依据
- 更新后的标题与导航：`index.html:6,11-17`
- 工具路由映射：`ai-invest/index.html:1`
- 会话脚本与入口控制：`auth.js:1`

请确认后按上述步骤操作，我将继续协助你完成验证与必要的项目设置调整。